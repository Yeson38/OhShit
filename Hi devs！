### 一、 项目背景与痛点深度剖析
在运维、开发及数据处理场景中，`rm -rf *`、`dd` 烧录失误或 `> file` 意外覆盖，堪称“删库跑路”悲剧的导火索。现有解决方案存在明显缺陷：

- **原生 `rm -i`**：逐文件确认效率极低，且**无法显示总数量与总大小**，用户无法评估损失范围。
- **管道忽视**：`rm -rf *` 与 `find` 或 `xargs` 组合时，`alias` 拦截往往失效。
- **疲劳操作**：深夜加班时，单纯的 `yes/no` 确认极易形成肌肉记忆，形同虚设。

本项目的核心价值在于**将“事后懊悔”转化为“事中强制认知”**，通过量化视觉冲击与随机挑战认证，从根本上杜绝无意识高危操作。

---

### 二、 产品定义与核心防护机制
**产品形态**：基于 Python（标准库）编写的高权限包装器，或编译为 Go 单二进制，通过 `alias` 及符号链接无缝替换系统危险命令。

**核心“三阶防护逻辑”**：

1. **参数预演与风险量化**：劫持命令后，递归展开通配符与路径，生成**受影响文件列表**。计算并高亮展示 **文件总数**（如 1,284 个）与**总大小**（如 15.6 GiB）。
2. **红色高威慑警告框**：在终端打印带边框的红色 ASCII 告警，强制输出**前 5 个文件名**（让用户肉眼确认是否为目标目录），从心理层面制造“暂停”间隙。
3. **动态随机文件名验证（防呆核心）**：系统随机选取目标目录下的一个真实文件名，要求用户**精确键入**。此举强制用户必须用眼扫描列表，比输入固定字符 `yes` 防呆效率提升 90% 以上。

**附加爽点**：支持 `~/.danger-whitelist` 白名单（如 `/tmp`、`/var/cache`）放行临时目录；支持 `DANGER_FORCE=1` 环境变量一键免打扰，兼顾 CI/CD 自动化场景。

---

### 三、 多系统兼容性技术架构设计
为覆盖 Linux、macOS 及 Windows（WSL/ PowerShell）生态，架构需直面底层差异：

| 差异维度 | Linux (GNU) | macOS (BSD) | Windows |
| :--- | :--- | :--- | :--- |
| **原生命令** | `rm`，`dd` | `rm`，`dd` | `Remove-Item`（需适配） |
| **参数风格** | 支持 `--force` 长选项 | 仅支持 `-f` 短选项 | 以 `-Recurse` 等为主 |
| **路径规则** | 根 `/`，大小写**敏感** | 根 `/`，默认大小写**不敏感**（APFS） | 盘符 `C:\`，大小写**不敏感** |
| **Shell环境** | Bash / Zsh | Zsh（默认） | PowerShell / CMD |

**技术应对策略**：

- **系统嗅探**：通过 `platform.system()` 识别内核，动态注入不同的原生命令映射表与参数过滤规则。
- **路径归一化**：统一使用 Python `pathlib` 处理路径拼接，自动适配反斜杠与正斜杠；文件匹配（glob）根据系统类型动态决定是否开启 `case_sensitive` 参数。
- **执行引擎**：拦截通过后，Linux/macOS 调用 `subprocess` 执行原生 `rm`；Windows 调用 `Remove-Item` 或调用 `shutil.rmtree`（跨平台兼容）。

---

### 四、 详细开发路线图（Roadmap）
采用敏捷迭代，**2 个开发日**交付可演示 MVP，并嵌入跨平台测试：

- **Day 1（核心引擎 + 跨平台预演）**
  - **上午**：搭建 CLI 框架（`argparse`），实现系统类型检测与命令别名绑定。构建文件预演引擎，利用 `os.walk` 递归统计数量及大小，重点解决 Linux 与 macOS 参数解析兼容性。
  - **下午**：实现终端彩色警告 UI（`colorama` 或 ANSI 转义）。编写随机文件名提取与交互验证逻辑，处理重试、取消（Ctrl+C）及空格文件名边缘情况。
- **Day 2（策略配置、打包与分发）**
  - **上午**：开发白名单解析与环境变量读取模块。编写**一键安装脚本**（Linux/macOS 用 Bash，Windows 用 PowerShell），自动备份原命令并写入 `.bashrc` / `.zshrc` / `$PROFILE`。
  - **下午**：**全平台回归测试**（GitHub Actions 或 Docker 容器），涵盖 `*` 通配符、隐藏文件及符号链接场景。使用 PyInstaller 打包为独立二进制，简化分发。
  - **晚间**：产出 `README.md`、演示 Asciinema 终端录像，准备 V2EX / 知乎“血泪史”共鸣文案。

---

### 五、 安装与运维计划
- **零门槛安装**：`curl -fsSL [url]/install.sh | bash` 或 `pip install danger-guard`。
- **无感卸载**：安装脚本生成还原点，执行 `uninstall` 即可恢复系统原生命令。
- **日志审计（拓展）**：预留接口记录每一次拦截操作至 `~/.danger.log`，便于事后追溯“手滑”瞬间。

---

### 六、 社会价值与推广共振点
此工具不仅救下毕业论文、数据库备份，更填补了 **“安全培训与肌肉记忆之间的鸿沟”** 。推广时可借助经典案例（如 Steam 游戏误删、`/usr` 目录权限错乱）引发极客共鸣。其开源属性可吸引社区贡献其他危险命令（如 `chmod 000`、`mkfs`）的拦截规则，形成“Linux 安全护盾”生态。

---

### 七、 结语
本项目虽小，却直击高频刚需。通过严谨的预演算法、创新的随机挑战认证机制，以及扎实的跨平台适配，两天内即可打造一款程序员必备的“防手滑”利器。立即启动，救赎无数熬夜删错文件的灵魂！
